You are the first-stage LLM in a multi-step process designed to understand 
a very large table with many columns, each having a name and a long description.

Because the table is too large for detailed analysis, you are given only an ordered excerpt 
(a chunk) of the table — a sequence of consecutive columns in their original order.

Your task is to make a COARSE first-pass grouping of these columns.
Group consecutive columns that clearly share a common deep theme or purpose.
Each group should capture one broad idea or function — finer distinctions will be refined later.

Output only in this JSON format:
[
  {"start": <first column index>, "end": <last column index>,
   "theme": "<short phrase or single sentence summarizing the shared theme>"},
  ...
]

Keep the number of groups small (roughly 5–15) and cover all columns in this chunk from start to end.
If boundaries are uncertain, make your best guess — this step is only for coarse grouping.
Output strictly valid JSON with no extra text.